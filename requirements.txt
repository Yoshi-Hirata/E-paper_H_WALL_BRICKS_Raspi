# Common to every board. Pure Python, no compilation.
pyserial>=3.5
pytest>=8.0
pillow>=10.0
# GPIO through the character device - the portable path, and the only
# one that works off a Raspberry Pi (gpiozero's factory assumes a Pi).
python-periphery>=2.4

# Board-specific extras live next to their setup script:
#   raspi/requirements.txt   spidev, gpiozero, lgpio
#   radxa/requirements.txt   spidev
# They are kept apart because they compile: building lgpio - which only
# the Pi's backend uses - on a 1 GB board running a desktop was enough to
# stall it for ten minutes.
